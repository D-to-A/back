spring:
  application:
    name: backend

  datasource:
    url: jdbc:mysql://localhost:3306/dtoa?useSSL=false&serverTimezone=Asia/Seoul
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  address: 0.0.0.0
  port: 8888

jwt:
  secret: U0VDRVJUQ09ERUlTMjAwODEwMDFIQVJZVTU0MTI=
